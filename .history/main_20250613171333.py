from playwright.sync_api import sync_playwright, Playwright,a

def run(playwright: Playwright):
    chromium = playwright.chromium
    print(chromium)
    api_request.new
    pass



with sync_playwright() as playwright:
    run(playwright)